<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ucsc.ettendance">

    <uses-sdk android:minSdkVersion="16" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label=""
            android:screenOrientation="portrait" />
        <activity
            android:name=".SignUp"
            android:label=""
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddClass"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MyClasses"
            android:label=""
            android:screenOrientation="portrait" />
        <activity
            android:name=".Pmain"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".PaddClass"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pClassPage"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity android:name=".classPage" />
        <activity android:name=".splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CheckInPage" />
        <activity
            android:name=".DayView"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pDayCode"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pPresentStudents"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pAbsentStudents"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCaW6wyjE7FeSMsk4GLz4y8HOSgjto5OrQ" />

        <activity
            android:name=".pMap"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pSetAnnouncement"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity
            android:name=".pAnnouncementPage"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue" />
        <activity android:name=".studentHelp" />
        <activity
            android:name=".pHelp"
            android:label=""
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeBlue">

        </activity>
    </application>

</manifest>